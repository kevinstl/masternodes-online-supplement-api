apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/kevinstl/jx-jhipster-microservice-template:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/kevinstl/jx-jhipster-microservice-template:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-jhipster-microservice-template
        chartPath: charts/jx-jhipster-microservice-template
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/kevinstl/jx-jhipster-microservice-template'
          image.tag: '{{.DIGEST_HEX}}'
