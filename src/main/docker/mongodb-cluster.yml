version: '2'
services:
    masternodesonlinesupplementeapi-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb masternodesonlinesupplementeapi-mongodb-config
    masternodesonlinesupplementeapi-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    masternodesonlinesupplementeapi-mongodb-config:
        image: mongo:4.0.2
        container_name: masternodesonlinesupplementeapi-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
